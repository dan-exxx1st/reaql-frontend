import React from 'react';
import { DialogHeaderBackdrop } from 'components/common';
import { storiesOf } from '@storybook/react';

storiesOf('Common/ Dialog header backdrop', module).add('default', () => (
    <DialogHeaderBackdrop />
));
