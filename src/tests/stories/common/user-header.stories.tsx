import React from 'react';
import { storiesOf } from '@storybook/react';
import UserHeader from 'components/common/UserHeader';

storiesOf('Common/ UserHeader', module).add('default', () => <UserHeader />);
