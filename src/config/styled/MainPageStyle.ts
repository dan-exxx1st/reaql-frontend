import styled from 'styled-components';
import { Wrapper } from 'components/UI';

export const StyledIndexWrapper = styled(Wrapper)`
    margin: 0;
    padding: 0;
    justify-content: center;
    align-items: center;
    background: #edeef0;
    width: 100vw;
    height: 100vh;
`;
