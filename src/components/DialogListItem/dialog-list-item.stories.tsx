import React from 'react';
import DialogListItem from '.';

export default {
	title: 'Dialog list item',
};

export const DefaultDialogListItem = () => (
	<DialogListItem>Default dialog list item</DialogListItem>
);
