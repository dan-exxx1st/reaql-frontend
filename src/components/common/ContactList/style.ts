import styled from 'styled-components';
import { Wrapper } from 'components/UI';
import { ContactItem } from '..';

export const StyledContactListWrapper = styled(Wrapper)``;

export const StyledContactListItem = styled(ContactItem)``;
