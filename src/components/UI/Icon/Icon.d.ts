declare namespace NIcon {
    import { IconName } from 'lib/types';
    interface IProps {
        iconName: IconName;
        width?: string;
        height?: string;
        className?: string;
    }
}
